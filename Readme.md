This is the code base for the Heresay -> LittlePrinter demo, based on the code from $ git clone git://github.com/roylines/node-littleprinter-example.git

This uses the current Heresay API which is temporary.